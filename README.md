# Salary-dataset
Salary Prediction using Linear Regression. This project predicts salary based on years of experience using a simple ML model. It includes data preprocessing, train-test split, model training, and evaluation. Model shows good performance with R² score ~0.90. Libraries used: Pandas, NumPy, Scikit-learn, Matplotlib.
